# История — teremok

Append-only. Новые записи сверху. Формат записи:

```
## ГГГГ-ММ-ДД — заголовок
- Сделано:
- Решения:
- Проблемы:
- Дальше:
```

## 2026-09-05 — проект заведён
- Сделано: создан скелет по шаблону оператора, `git init`, env-файл.
- Решения: —
- Проблемы: —
- Дальше: заполнить `CLAUDE.md`, положить спек в `spec/`.

## 2026-09-05 — от спеки до рабочего ввода за один заход

- Старый сайт снесён, история репозитория заменена (`push --force`:
  первый push молча отклонился как non-fast-forward, `-q` спрятал отказ,
  прод остался старым — проверять `git ls-remote` после push, а не верить тишине).
- Схема применена через пулер `us-west-2:6543`: прямой хост отдаёт только
  IPv6, пулеры привязаны к региону, «East US» в интерфейсе оказался Oregon.
- Сквозная проверка на живой базе: объект → фото → распознавание →
  запись с чеком в storage → `site_totals`. Reimbursable не уменьшил прибыль.
- Распознавание на синтетическом чеке: магазин, итог 198.01 (после налога),
  дата, 4 позиции, вид `reimbursable` по магазину.
