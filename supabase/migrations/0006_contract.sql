-- Сколько управляющая компания должна за саму работу по объекту. Возмещаемое
-- за материалы считается отдельно: это разные долги и закрываются по-разному
-- (работа — счётом, материалы — чеками).
alter table sites add column contract_amount numeric(12,2);
